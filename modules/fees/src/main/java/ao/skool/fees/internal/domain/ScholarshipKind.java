package ao.skool.fees.internal.domain;

public enum ScholarshipKind {
    FULL,          // 100% discount
    PERCENTAGE,    // uses percentage column
    FIXED          // uses fixed_amount column
}
